const screen=document.querySelector('.phone-screen');const image=document.querySelector('#reader-image');const title=document.querySelector('#reader-title');document.querySelectorAll('.note').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.note.is-active')?.classList.remove('is-active');button.classList.add('is-active');image.style.opacity='0';setTimeout(()=>{image.src=`/case-media/fantawild/fantawild-${button.dataset.comic}.webp`;image.alt=button.dataset.title+'长条漫画';title.textContent=button.dataset.title;screen.scrollTo({top:0,behavior:'smooth'});image.style.opacity='1'},180)}));image.style.transition='opacity .2s';screen?.addEventListener('wheel',event=>{if(Math.abs(event.deltaY)>Math.abs(event.deltaX)){const atTop=screen.scrollTop<=0&&event.deltaY<0;const atBottom=Math.ceil(screen.scrollTop+screen.clientHeight)>=screen.scrollHeight&&event.deltaY>0;if(!atTop&&!atBottom){event.preventDefault();screen.scrollTop+=event.deltaY}}},{passive:false});
